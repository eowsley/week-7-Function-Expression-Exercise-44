/* function that returns the square of a number. define the function as a 
function expression, stored in a variable called square. */

let mathSquare = square => square * square;

console.log(mathSquare(3))